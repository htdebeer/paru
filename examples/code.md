Next follows a bit of code:

indented:

    var a = 0;
    while (a > -100) {
        console.log(a);
        a--;
    }

fenced:

~~~ {#mycode .javascript}
var b = "asdasd";
if (b.length < 10) {
    console.error("Too longk");
}
~~~~
